server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    show-sql: false
  application:
    name: rest-spring
springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true
